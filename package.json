{
  "name": "new-charges",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && yarn start",
    "backend": "cd backend && yarn start",
    "backend-install": "cd backend && yarn",
    "frontend-install": "cd frontend && yarn",
    "dev": "concurrently \"yarn backend\" \"yarn frontend\"",
    "install-dev": "concurrently \"yarn backend-install\" \"yarn client-install\""
  },
  "dependencies": {
    "concurrently": "^7.2.2"
  }
}
